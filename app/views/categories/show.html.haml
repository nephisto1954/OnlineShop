.container
  .home
    .title
      %h1= @category.name
      %h4= link_to "Add a product", new_category_product_path(@category)
    = render "list"
    = link_to "Back to all categories", categories_path
